version: "3.8"

services:
  chatbot-database:
    ports:
      - "6000:6000"
    environment:
      - FLASK_ENV=development
      - SQLALCHEMY_DATABASE_URI=${SQLALCHEMY_DATABASE_URI}
    restart: always
